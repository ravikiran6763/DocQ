!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!Payment Tables!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

CREATE TABLE patient_wallet (
id INT AUTO_INCREMENT PRIMARY KEY,
patientPhone varchar(30), 
consultationId int,
consultDatetime datetime,
transactionId int(5) NOT NULL,
transactionDatetime datetime,
amountCredited int(5) NOT NULL,
creditedDatetime datetime,
amountDeducted int(5) NOT NULL,
debitedDatetime datetime
);

insert into patient_wallet (patientPhone,consultationId,consultDatetime,amountDeducted,debitedDatetime) values('9844992181','25111',now(),250,now());
insert into patient_wallet (patientPhone,transactionId,transactionDatetime,amountCredited) values('9844992181','10111',now(),500);

CREATE TABLE patientPaymentTransaction(
id INT AUTO_INCREMENT PRIMARY KEY,
consultationId int,
consultDatetime datetime,
amountDebited int(5) NOT NULL,
debitedFrom varchar(30),
creditedTO varchar(30),
debitedDatetime datetime
);


insert into patientPaymentTransaction(consultationId,consultDatetime,amountDebited,debitedFrom,creditedTO,debitedDatetime)values(123654,now(),250,'9844992181','9738162020',now());

CREATE TABLE patientQueries(
id int AUTO_INCREMENT PRIMARY KEY,
phNo varchar(30),
query text,
dateTime date
);

CREATE TABLE patientCallback(
id int AUTO_INCREMENT PRIMARY KEY,
phNo varchar(30),
query text,
dateTime date
);


CREATE TABLE wallet (
  id bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  users_id int(11) unsigned NOT NULL,
  flag enum('gate','order-','refunded','deduction-','transfer') NOT NULL COMMENT 'dash indicates a negative value',
  cash decimal(14,2) NOT NULL,
  description varchar(255) NOT NULL COMMENT 'note',
  ts timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY users_id (users_id),
  CONSTRAINT wallet_ibfk_2 FOREIGN KEY (users_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;


